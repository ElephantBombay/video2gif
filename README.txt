README

1. npm init
2. Installs:
    npm i fluent-ffmpeg
    npm i @ffmpeg-installer/ffmpeg @ffprobe-installer/ffprobe
    npm i multer (middleware for handling multipart/form-data when using an upload form)
    npm i express
    npm install --save-dev nodemon
    npm i aws-sdk
3. mkdir "thumnbails"
4. code "index.js"
5. npm run devStart
